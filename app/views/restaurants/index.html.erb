<div class="container">
  <h1>All Restaurants: </h1>
  <hr>
  <%= link_to "Add a new restaurant", new_restaurant_path%>
  <hr>
  <% @restaurants.each do |restaurant| %>
  <li><%= restaurant.name %></li>
  <%= link_to "Restaurant Details", restaurant_path(restaurant) %>
  <hr>
  <% end %>
</div>
